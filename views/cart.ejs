<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8"/><meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Carrito — Vinyllector</title>
  <link rel="stylesheet" href="/css/style.css"/>
</head>
<body>
  <%- include('partials/navbar') %>
  <main class="container card">
    <h1>Carrito</h1>
    <%- include('partials/flash', { msg }) %>

    <% if (!items || !items.length) { %>
      <p class="muted">Tu carrito está vacío.</p>
    <% } else { %>
      <table>
        <thead><tr><th>Producto</th><th class="right">Precio</th><th class="right">Cantidad</th><th class="right">Subtotal</th></tr></thead>
        <tbody>
          <% items.forEach(it => { %>
            <tr>
              <td><%= it.nombre %></td>
              <td class="right">$<%= it.precio.toLocaleString('es-CL') %></td>
              <td class="right"><%= it.cantidad %></td>
              <td class="right">$<%= (it.precio*it.cantidad).toLocaleString('es-CL') %></td>
            </tr>
          <% }) %>
        </tbody>
        <tfoot>
          <tr><th colspan="3" class="right">Total</th><th class="right">$<%= total.toLocaleString('es-CL') %></th></tr>
        </tfoot>
      </table>

      <form method="post" action="/cart/checkout" style="margin-top:12px; display:inline-block">
        <button class="btn">Confirmar compra</button>
      </form>
      <form method="post" action="/cart/clear" style="margin-left:8px; display:inline-block">
        <button class="btn ghost">Vaciar carrito</button>
      </form>
    <% } %>
  </main>
  <%- include('partials/footer') %>
</body>
</html>
